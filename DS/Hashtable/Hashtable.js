class Hastable {
    constructor(size = 53) {
        this.keyValueMap = new Array(size);
    }
    _hash(key) {
        let hasedKey;
        // some hasing functions
        return hasedKey;
    }
    set(key,value) {
        let index = this._hash(key);
    }

    get(index) {

    }
}